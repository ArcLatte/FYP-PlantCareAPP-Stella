# CPU-only PyTorch wheels — the CUDA build is ~2.5 GB and useless on a CPU
# server. The +cpu builds come from PyTorch's CPU index below. Versions match
# the ones the models were trained/saved with so checkpoints load cleanly.
--extra-index-url https://download.pytorch.org/whl/cpu

Django==6.0.5
djangorestframework==3.17.1
psycopg2-binary==2.9.12
Pillow==12.2.0
python-dotenv==1.2.2

# Production server + static files + DB URL parsing
gunicorn==23.0.0
whitenoise==6.8.2
dj-database-url==2.3.0

torch==2.12.0+cpu
torchvision==0.27.0+cpu
